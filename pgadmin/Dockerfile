# Verwende das offizielle pgAdmin4 Docker Image
# Dieses Image ist bereits vollständig konfiguriert.
FROM dpage/pgadmin4:latest

# WICHTIG:
# Die Konfiguration erfolgt jetzt über Umgebungsvariablen in deiner Render.com-Umgebung.
# Setze dort die folgenden Variablen:
#
# - PGADMIN_DEFAULT_EMAIL:    deine-email@example.com
# - PGADMIN_DEFAULT_PASSWORD:   dein-sicheres-passwort
# - PGADMIN_LISTEN_PORT:      80 (oder was Render.com für den internen Port vorgibt)
#
# Alle anderen Umgebungsvariablen aus dem alten Dockerfile sind nicht mehr nötig,
# da das offizielle Image sinnvolle Standardwerte hat.
